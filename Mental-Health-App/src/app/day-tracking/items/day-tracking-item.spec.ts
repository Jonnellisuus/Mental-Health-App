import { DayTrackingItem } from './day-tracking-item';

describe('DayTrackingItem', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new DayTrackingItem()).toBeTruthy();
  });
});
//
