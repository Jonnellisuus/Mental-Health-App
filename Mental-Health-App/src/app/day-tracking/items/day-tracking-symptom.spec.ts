import { DayTrackingSymptom } from './day-tracking-symptom';

describe('DayTrackingSymptom', () => {
  it('should create an instance', () => {
    expect(new DayTrackingSymptom()).toBeTruthy();
  });
});
