import { DayTrackingContact } from './day-tracking-contact';

describe('DayTrackingContact', () => {
  it('should create an instance', () => {
    expect(new DayTrackingContact()).toBeTruthy();
  });
});
